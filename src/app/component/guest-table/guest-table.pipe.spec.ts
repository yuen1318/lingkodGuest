import { GuestTablePipe } from './guest-table.pipe';

describe('GuestTablePipe', () => {
  it('create an instance', () => {
    const pipe = new GuestTablePipe();
    expect(pipe).toBeTruthy();
  });
});
